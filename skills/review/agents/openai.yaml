interface:
  display_name: "レビュー"
  short_description: "差分レビューと検証観点整理"
  default_prompt: "$review を使って、staged/unstaged/untracked の差分を確認し、plan と test 結果があれば照合して。findings first で重大度順に指摘し、検証コマンドは実行しないで。"
