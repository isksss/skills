# plan workflow

- 依頼、成功条件、制約、既存差分、局所ルールを確認する。
- `research` の事実と `grilling` の決定事項を読み、未確認事項と推測を分ける。
- 対象ファイル、既存の参照実装、依存、公開 API、データ移行の影響を必要な範囲だけ調べる。
- 小さく明確な変更は短い計画にし、不要な設計や調査を追加しない。
- 変更対象をファイル単位で示し、実装順序、再利用する既存パターン、追加するテストコードを決める。
- lint、typecheck、test、build など test で実行するコマンドを、変更範囲とリスクに応じて選ぶ。
- 計画を左右する不明点だけ質問し、コードから確認できることは質問しない。
- 出力は「目的」「変更対象」「実装手順」「検証」「リスク」「未確認事項」に分ける。
